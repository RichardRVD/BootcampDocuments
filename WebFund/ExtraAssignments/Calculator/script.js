var displayDiv = document.getElementById("display");
displayDiv.innerText = "Some new value";

function press(Element){
    var numPress = Element
    numPress;
    displayDiv.innerText = numPress;
    return numPress;
}  
function setOP(element){
    var operator = element;
    operator;
    displayDiv.innerText = displayDiv.innerText + operator;
    return operator;
}
function calculate(){

}